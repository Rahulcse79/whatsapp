apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wa-service.fullname" . }}-config
  labels:
    {{- include "wa-service.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
