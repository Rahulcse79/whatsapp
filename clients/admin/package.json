{
  "name": "@wa/admin",
  "version": "0.0.0",
  "private": true,
  "description": "WhatsApp V2 admin console — internal React SPA over the /admin/v1 REST API (reports, users, feature flags, audit). OIDC bearer auth; no user content ever crosses this plane.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p .",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "~5.7.3",
    "vite": "^6.0.0",
    "vitest": "^2.1.8"
  }
}
