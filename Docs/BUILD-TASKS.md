# Build Tasks — full feature runner (V2 completion → V3/V4)

Run these **one at a time**, in order. Each task is a self-contained, shippable
increment: copy its `PROMPT>` line to start it. Mirrors the style of
`Docs/TASKS.txt` (which built the V2 backend, T0–T4).

- `[ ]` open · `[~]` in progress · `[x]` done
- Finishing a task: flip its box, commit + push, then run the next.
- Status of the underlying features: `Docs/FEATURE-CHECKLIST.md` (D/S/C columns).

---

## Architect's reconciliation (read once)

The V2 platform is **already built and production-grade** at the architecture +
backend layer (5 Go services, Postgres/Valkey/NATS/MinIO/LiveKit, E2EE, Helm/
ArgoCD/CI, observability). **These tasks are mostly the client-UI frontier plus
new features** — do NOT rebuild the backend. Where this spec names tech that
differs from V2's decided stack, the pragmatic call:

- **E2EE is non-negotiable and constrains "AI/search over messages."** The
  server cannot read content. So: AI (translation, summaries, smart replies,
  transcription, moderation) runs **on-device or on explicitly user-disclosed
  content**; full-text message search stays **client-side (SQLite FTS5)**.
  Server-side Elasticsearch is only for **public/metadata** surfaces (channels,
  user/username discovery, public communities).
- **Kafka/RabbitMQ → NATS JetStream** already fills the event-bus/queue role at
  20k concurrent. Add Kafka only if a task's scale profile proves NATS is the
  bottleneck (a dedicated task below evaluates this — don't add it speculatively).
- **Cassandra → partitioned PostgreSQL** already serves the inbox at target
  scale. Add Cassandra only behind the same evidence gate (task P.03).
- **New realtime fan-out (channels/communities/webinars)** rides the existing
  WS gateway + NATS, not a parallel stack.

Everything else in the spec is additive UI + new bounded contexts.

---

# PHASE 5 — Finish the V2 client experience (make what's built usable)

The backend for all of this exists; these wire the mobile + web UI end-to-end.

- [x] **T5.00  Fix live message delivery — THE BLOCKER** *(done — real 1:1 chat works)*
  ROOT CAUSE (two bugs): (1) wire-codec mismatch — the web transport sent frames as JSON but ws-gateway decodes binary protobuf, so the handshake was rejected with 4400 "expected hello frame" (messages never left the client). (2) `newId()` produced a fake `ts-rand` id, but `chat.Accept` requires a real UUIDv7 (`internal/platform/id.Parse`), so sends were rejected. FIX: wired `@wa/proto-types` (generated protobuf-es) + `@bufbuild/protobuf` into web and rewrote `wsTransport.ts` to a binary protobuf codec (client-core frames ↔ wsv1.Frame); made `newId()` emit spec UUIDv7. VERIFIED LIVE in two browser sessions (localhost + 127.0.0.1 origins): A "add-by-number" → real shared conversation → **A→B and B→A messages delivered and decrypted end-to-end**. NEXT: bring the same protobuf transport to mobile (rn wsTransport), then T5.01+.
- [~] **T5.01  Real 1:1 messaging** *(web mostly done this session; awaits T5.00; mobile next)*
  PROMPT> WEB done this session (commits 3cdef47+6b16f0b): add-by-number "new chat" → POST /v1/conversations/direct, conversation-shared AES-GCM dev cipher, decrypt-on-receive — VERIFIED LIVE that A opens the real shared conversation with B by number; live message exchange is gated on T5.00. NEXT: mobile parity (convCrypto.ts scaffolded, uncommitted), then verify two devices exchange readable messages.
- [x] **T5.02  Live thread updates + chat list refresh** *(web done; mobile with T5.01)*
  DONE (web): AppServices now has an `onChange` emitter fired on inbound batches, send-acks, and sends; the Thread + ChatList screens subscribe to it and re-fetch, replacing the old 1s/1.5s busy-poll with instant event-driven refresh (a 5s safety poll remains). VERIFIED LIVE: B sitting in the open thread saw A's second message ("LIVE UPDATE msg2 ⚡") appear instantly, no send/navigation. Mobile parity rides T5.01.
- [x] **T5.03  Message status + read receipts UI** *(web done; mobile with T5.01)*
  DONE (web): full ✓ / ✓✓ / ✓✓-blue ticks. client-core gained a server→client `Receipt` frame + `WsClient.onReceipt` + `MemoryMessageRepo.markReceipt` (monotonic state rank sending<sent<delivered<read). Web sends a DELIVERED receipt when it persists an inbox batch, and a READ receipt when the thread is on screen (`AppServices.markRead`); incoming receipts upgrade my bubbles' state; `StatusTicks` renders them. VERIFIED LIVE: A→B "tick test" showed ✓ then ✓✓ (B received, chat-list only) then ✓✓ **blue** once B opened the thread. Privacy-gating of receipts (FR-USER-02: last-seen/receipts everyone|contacts|nobody) is a later refinement — currently always sent in dev. Mobile parity rides T5.01.
- [x] **T5.04  Typing + presence UI** *(web done + verified live; mobile with T5.01)*
  DONE (web): client-core gained bidirectional `Typing` + `PresenceSub`/`PresenceUpdate` frames + `WsClient.onTyping`/`onPresence`; the web protobuf transport encodes/decodes them; AppServices tracks `typingByConv`/`presenceByUser`, sends throttled typing (1/3s) on compose + a stop-typing on send, and subscribes to the peer's presence on thread open (peer resolved from `startDirectChat` for the initiator and from the inbound message's `senderUserId` for the recipient). The Thread header renders a `statusLine`: **typing… › online › last seen …**. Server side was already wired (ws-gateway presenceConnect/Disconnect publish online/last-seen with flap-debounce; handleTyping → PublishTyping; handlePresenceSub → subscribe+snapshot). VERIFIED LIVE (two sessions, localhost + 127.0.0.1): A↔B both show **online**; A types → B header shows **typing…**, and B types → A shows **typing…**; alongside the ✓✓ receipts. Privacy-gating of presence (FR-USER-02) stays AllowAll in dev — a later refinement. Mobile parity rides T5.01.
- [~] **T5.05  Message actions (menu)** *(web done + verified live; forward + reactions deferred to T5.05b; mobile with T5.01)*
  DONE (web): each bubble has a **⋯ action menu** — reply, copy, star, pin, edit (mine, <15 m), delete-for-everyone (mine, <48 h), delete-for-me. Built the missing overlay-SEND infra: `OutgoingDraft`/`MsgSend` now carry `kind`+`overlayTarget`, `MemoryMessageRepo.enqueueOutgoing` applies edit/delete optimistically to the target (no new bubble) and `pendingSends` emits the right overlay kind; inbound `OVERLAY_EDIT` rewrites the body (via a new `OverlayApply.msgUuid` that keys the decrypted edit text); `deleteForMe` hides locally. Reply carries a `QuotedRef` in the sealed body (media-pipeline `encode/parseTextMessage`), rendered as a quote box. Worker RPC + web AppServices methods (editMessage/deleteForEveryone/deleteForMe/togglePin/toggleStar); Thread UI has an edit/reply composer bar + edited/star/pin indicators. VERIFIED LIVE (localhost session): sent a message → **edited it** (bubble shows new text + "(edited)"), **starred it** (⭐ + menu flips to Unstar), **replied** (quote box renders the original above the reply), **deleted-for-everyone** ("This message was deleted"). The peer-side overlay apply is unit-tested (client-core 54 + media-pipeline 67 green) and rides the same wire path proven 2-session in T5.00–T5.04. **DEFERRED to T5.05b:** forward (needs a conversation picker) + reactions (needs a reaction storage model + REACTION-overlay render). Mobile parity rides T5.01.
- [x] **T5.05b  Forward + reactions** *(web done; mobile parity rides T5.01)*
  PROMPT> forward a message to another conversation (picker) + emoji reactions (single + multiple) using the REACTION overlay kind — store + render on both bubbles. Web then mobile.
  DONE (web): completes the two T5.05-deferred actions on the proven overlay + send paths. **Reactions** ride the existing **REACTION overlay kind** (was routed by `planInboxBatch`/`isOverlayKind` but never applied): media-pipeline gained `encodeReaction`/`parseReaction` ({t:"react",emoji,op}); client-core `ThreadMessage` gained `reactions: ReactionSummary[]` (emoji/count/mine), and `MemoryMessageRepo` now folds reactions via a pure `applyReaction` — my outgoing REACTION applies optimistically (mine+count, no new bubble) and toggles off on remove, an inbound peer REACTION increments the tally (count includes me; inbound is always the peer, so `mine` only set by my own send), removing to zero drops the emoji. Worker `enqueueOverlay` maps a new `"react"` kind → `MsgKind.REACTION` + seals the emoji body; `AppServices.react(conv,msgUuid,emoji,op)` (op decided from the emoji's current mine-state). UI: a **quick-emoji row** (👍❤️😂😮😢🙏) atop the bubble ⋯ menu + **tappable reaction chips** under the bubble (highlighted when mine, count shown for >1). **Forward:** `AppServices.forwardMessage(src,msgUuid,target)` re-sends the message's content (text with link preview, or media re-sealed under the target's key — same uploaded object; reply quote dropped) via the normal enqueue path; a **↪ Forward** menu item opens a **conversation picker** sheet (excludes the current chat). VERIFIED: media-pipeline 71 (4 new reaction-codec tests) + client-core 57 (3 new reaction-apply tests: inbound peer, my optimistic toggle, my+peer aggregate) green; all 4 client workspaces typecheck; web app loads live with zero console errors. **NOT re-verified this session:** the live 2-account react/forward click-through (same OTP/controlled-input automation friction as T5.15) — the reaction reducer is fully unit-tested and forward rides the proven send path, so verified-by-construction. The DB (SQLite) path returns `reactions: []` — mobile reactions land with T5.01. Mobile parity rides T5.01.
- [x] **T5.06  Media send in chat** *(web done + verified live; voice-note record + camera + compression/thumbnail deferred; mobile with T5.01)*
  DONE (web): composer **📎 attach** → file picker → `MediaPipeline.prepare` (encrypt via the file key + **resumable multipart upload** to media-svc) → `encodeMediaMessage` sealed in the conversation body → media bubble (render already existed). New `web/src/platform/mediaUpload.ts` (UploadTransport: create/presign/complete + direct-to-object-storage part PUT, **refreshes the token on 401**); `config.mediaBaseUrl` (media-svc is a separate :8082 deployable); download transport repointed to it too; `AppServices.sendMedia`. Unblocked 3 server-integration gaps found live: (1) **CORS on media-svc** — added the same dev-CORS core-api uses; (2) **QuotaService gRPC unbuilt** in core-api → wired a dev/offline **NoopQuota** (prod keeps the real client); (3) upload transport now refreshes the 10-min access token on 401. VERIFIED LIVE: injected a PNG → create→presign→**PUT to MinIO**→complete → the ciphertext object landed in the `media` bucket and a **media bubble rendered** in the thread. server build+vet+media tests green; web typecheck green. **DEFERRED:** voice-note record/playback, camera capture, client compression + thumbnail/blurhash (the pipeline ports are optional — upload is raw bytes today). Mobile parity rides T5.01.
- [x] **T5.07  Profile & privacy screens** *(web done + verified live; avatar image upload + per-user QR deferred; mobile with T5.01)*
  PROMPT> profile (username, display name, avatar, about), per-field privacy toggles (last-seen/avatar/about/receipts: everyone|contacts|nobody), block/unblock list, per-user QR. Both clients.
  DONE (web): new **`internal/profile`** bounded context (content-free — identity metadata only) over the existing `users` (username/display_name/about/privacy jsonb, migration 000002) + `blocks` (000004) tables. Service does the light validation clients can't be trusted with (display ≤100, about ≤200, username 3–30 `[a-z0-9_.]` case-folded, privacy value ∈ everyone|contacts|nobody, no self-block). REST (all bearer-gated): `GET/PUT /v1/me`, `PUT /v1/me/privacy`, `GET /v1/users/{id}` (public view, UUID-validated), `GET/POST/DELETE /v1/blocks`. Wired in `cmd/core-api/main.go`. Client: `AppServices` gained `getMyProfile`/`updateMyProfile` (409→"username taken"), `saveMyPrivacy`, `loadUserProfile`+`peerNameOf`+`nameForUser` (userId→display-name cache that re-renders open screens on arrival), `blockUser`/`unblockUser`/`getBlocked`, and a `fetch`-based `authedRequest` (GET/PUT/DELETE + refresh-on-401). New **Profile screen** (`👤 You` in the chat-list head): edit display-name/username/about, 4 privacy `<select>`s (optimistic save), and a blocked list with Unblock. Thread header + chat-list rows now show the **peer's display name** instead of the raw conversation id. VERIFIED LIVE: signed in, saved profile (`Rahul Singh` / `rahul.singh` / about → **persisted to Postgres**), flipped last-seen→nobody + read-receipts→contacts (**privacy jsonb persisted**), Blocked(0) renders. server build+vet+profile curl-suite green (GET empty→PUT→GET confirms, dup username→409 USERNAME_TAKEN, block[204]→list→unblock[204]); web typecheck green. **DEFERRED:** avatar **image** upload (needs the media pipeline) + per-user **QR** (needs a QR renderer/dep). Mobile parity rides T5.01.
- [x] **T5.08  Contacts screen + sync** *(web done + verified live; mobile address-book read with T5.01)*
  PROMPT> address-book sync (hashed handles → /v1/contacts/sync), matched-contacts list, search-by-username, favorites, invite link. Both clients.
  DONE (web): wired the existing **`internal/contacts`** backend (T1.09 — peppered-HMAC discovery, PG-trigram username search, favorites, capability-token invites; all bearer-gated) to a new **Contacts screen** (`👥 Contacts` in the chat-list head). `AppServices` gained `searchContacts` (debounced, ≥2 chars), `listFavorites`/`addFavorite`/`removeFavorite`, `syncPhones` (paste numbers → matched list; 429→"4×/day" message), `createInvite`, and `openDirectWithUser` (shared 1:1 opener extracted from `startDirectChat`, so search/favorite/matched rows all "Message" straight into a thread). Screen sections: **Find people** (username search → rows with ☆/★ favorite-toggle + Message), **Favorites(n)** (starred, loaded from server), **Find by phone** (textarea → `POST /v1/contacts/sync` → matched), **Invite a friend** (create link + copy-to-clipboard). Server sends **plaintext handles over TLS** and peppers+hashes them server-side (only the HMAC is persisted). VERIFIED LIVE (two accounts — `rahul.singh` + seeded `alice_test`): searched "alice"→both alices, starred `alice_test` (**★ synced across search+favorites, persisted across nav**), Message→opened the 1:1 with the header showing "Alice Tester", phone-sync of 3 numbers→2 matched / 1 unregistered correctly excluded, invite link minted (`https://wa.local/i/…`). web typecheck green. **DEFERRED:** real mobile **address-book** read + hashed sync rides T5.01; invite **landing page** (resolve-token → sign-up) is a public-web route for later.
- [x] **T5.09  Groups UI** *(web done + verified live incl. real 2-account group delivery; @mentions + QR image deferred; mobile with T5.01)*
  PROMPT> create/edit group, member list + roles (owner/admin/member, promote/demote), add/remove/leave, invite links + QR join, announcements mode, @mentions, pinned. Both clients.
  DONE (web + a server fix). **Server fix (critical):** the groups backend (T1.01) wrote only `group_members`, but the chat accept/fan-out pipeline resolves recipients from `conversation_members` — so group sends would 403 `STATE_NOT_MEMBER` and deliver to nobody. Bridged in `groups/adapters/pg.go`: `CreateGroup` now also inserts the `conversations` row (id == group id, kind=1, group_id FK for ON-DELETE-CASCADE) and mirrors members into `conversation_members`; `AddMembers`/`RemoveMember`(=Leave)/`JoinViaInvite` keep the mirror in sync (helper `mirrorMembers`). **Client:** `AppServices` gained `createGroup`, `loadGroup`/`groupOf`/`groupNameOf`/`ensureConversationKind` (group-vs-direct classifier cache — note a recipient's inbox sets peerByConv even for groups, so only a 404 means direct), `listGroupMembers`, `addGroupMembers`/`removeGroupMember`/`setGroupRole`, `updateGroupInfo`/`setGroupSettings`, `leaveGroup`/`deleteGroup`, `createGroupInvite`/`joinGroup`. New **CreateGroup** screen (`＋👥 Group`): name + description + username-search member picker. New **GroupInfoScreen** (tap the thread header / ℹ️): role-gated settings (announcements toggle + who-can-post + who-can-edit-info), member list with role badges + promote/demote (owner) + remove (admin+), add-members search, invite link (create+copy), leave (all) / delete (owner). **Thread** now classifies the conversation: group name + ℹ️ in the header, and the composer is replaced with "📢 Only admins can post" when announcements/admin-only and I'm not an admin (server also enforces via `CanPost`). Chat-list rows show `👥 <group name>`. VERIFIED LIVE (two browsers, rahul.singh owner + alice_test): created "Weekend Trip", **messages fan out to both members' device inboxes** (confirmed in `message_inbox`) and render decrypted on the peer; **bidirectional** send/receive with ✓✓ receipts; group name in list+header; owner toggled **announcements** → member's composer disabled live (re-fetch on reopen); **promote** alice→admin persisted (role 1). server build+vet+groups tests green; web typecheck green. **DEFERRED:** @mentions autocomplete, per-user/**group QR image** (backend returns the invite URL string; needs a QR renderer/dep), pinned-in-group specifics (the pin action already exists from T5.05), member-list pagination beyond 256. Mobile parity rides T5.01.
- [x] **T5.10  Calls UI** *(web voice+video live over real LiveKit; mobile voice+video wired — on-device verify pending; group grid deferred)*
  DONE (web): the 1:1 call UI (CallOverlay: outgoing/incoming ring accept/decline, connecting/connected, ended, controls camera/flip/screen-share/blur + End, local self-preview) wired to the REST control plane (`/v1/calls` create/answer/decline/rejoin) + the @wa/call-engine state machine + SFrame E2EE, plus the **call-history screen** (`GET /v1/calls/history`, 📞 Calls entry). Later commits (7a7c9a9, 0414da3) **wired real LiveKit** into the dev stack (`start.sh` runs a LiveKit SFU on :7880; core-api mints room join tokens with the dev key/secret) and replaced the web `stubRtc` with `liveKitRtc.ts` (mic on connect, remote track render, camera/screen republish, SFrame senders/receivers), plus the WS `dev.{id}.call` frame decode for cross-device ring.
  DONE (mobile — this pass): completed the in-progress RN call media. `app/_layout.tsx` now calls **`registerGlobals()`** at module load (installs the WebRTC globals — without it a call crashes on connect). `createLiveKitRnRtc` (rnCallMedia.ts) starts the native **AudioSession**, joins the SFU with the minted token, publishes the mic, and now **(re)publishes camera/screen** tracks + exposes `getRoom()`. `CallContext` injects the real **`@livekit/react-native-webrtc` `mediaDevices`** into the camera adapter (real capture + self-preview URL) and adds `remoteStreamURL()` (reads the remote participant's camera track). `CallOverlay` renders the **remote camera full-card + a mirrored local self-view** via `<RTCView>`. app.config.ts adds the LiveKit + react-native-webrtc Expo config plugins (minSdkVersion 24). VERIFIED: full workspace typecheck + mobile tests green; the shared control plane mints a valid **LiveKit video grant** (`POST /v1/calls` → `room_id` + `join_token` with roomJoin/canPublish/canSubscribe — curl-checked against the running SFU); web voice+video verified live earlier. **PENDING (needs a device):** on-device Android run of the RN media path (RTCView render, camera permission prompt) — not runnable in this environment. **SEAMS:** SFrame per-frame E2EE at the RN layer waits on react-native-webrtc encoded-transform support (calls use DTLS-SRTP hop-by-hop today); **group grid ≤32** deferred.
- [x] **T5.11  Stories/Status UI** *(web done + verified live incl. cross-account; content-decrypt = STORY_KEY seam; reactions deferred; mobile with T5.01)*
  PROMPT> status composer (image/video/text), story ring on chat list, viewer with tap-through, audience selector, view receipts + viewer list, reactions. Both clients.
  DONE (web): wired the existing **`internal/stories`** backend (T3.05 — E2EE **metadata-only**: the feed returns story_id/author/expiry/key_available, never content or even the media location, which ride the client-to-client STORY_KEY channel). `AppServices` gained `postStory`/`storyFeed`/`viewStory`/`storyViewers`/`deleteStory`, `uploadStoryMedia` (reuses the chat media pipeline → a real `media_ref` for image stories, satisfying the server's image-needs-media-ref rule), `myUserId` (JWT sub), and a local **dev content cache** (`saveStoryContent`/`loadStoryContent` in localStorage — own-story payload; cross-device content is the unwired STORY_KEY seam). New **Status screen** (`⭕ Status` in the chat-list head): "My status" + "Recent updates" grouped by author, a **composer** (text with bg-color swatches, or a photo ≤2 MB; audience = "My contacts" default or "Only share with…" username-picker → audience_override), and a full-screen **tap-through viewer** (per-story progress bars, 5 s auto-advance, left/right tap zones, delete own, and — for own stories — a "👁 Viewed by N" panel). Content renders text/image from the cache, else a 🔒 encrypted placeholder. VERIFIED LIVE (two accounts): rahul posted a purple text status → **persisted to Postgres** (audience_snapshot frozen), "My status · 1 update", viewer rendered the content + progress bar + view-receipt; **cross-account** — alice's feed included rahul's story (audience = author's contacts), alice's view returned 204, and rahul's **viewers list showed alice** (author's own view is not self-counted). web typecheck green. **DEFERRED:** **reactions/quick-reply** (no story-reaction endpoint — would DM the author), **video** status (same media path as image, untested), and cross-device **content decryption** (STORY_KEY distribution over WS). Mobile parity rides T5.01.
  DEV-RIG NOTE: repeated test logins exhaust the **OTP request rate limit** (429; clear `redis-cli KEYS 'rl:otp:*' | xargs redis-cli DEL`) and the **device limit** (403 STATE_DEVICE_LIMIT, 1 primary + 4 linked — each login mints a new device; clear with `DELETE FROM devices WHERE user_id IN (…)` for the test accounts). Both surfaced as "Couldn't sign in" and are auth-rig artifacts, not app bugs.
- [x] **T5.12  Multi-device linking + settings** *(web device-management done + verified live; QR-link/backup/account = documented seams; mobile with T5.01)*
  PROMPT> device list/rename/revoke, QR link a new device (crypto-wrapper deviceList), linked-device history bootstrap (sync-engine), encrypted backup create/restore UI, notification settings, account delete/export. Both clients.
  DONE (web): new **Settings screen** (`⚙️ Settings & devices` from the Profile page) over the existing **`internal/devices`** backend. `AppServices` gained `myDeviceId` (session), `listDevices`/`renameDevice`/`revokeDevice`. **Linked devices** section: lists every device with **primary / "this device" / platform** markers, inline **rename**, and **revoke** (own device → `logout()` + back to login; others → removed from the list). **Notifications** toggle wires the existing `registerWebPush`. **Link a device**, **chat backup**, and **account export/delete** render as documented seams (QR scan + signed-device-list cert = `@wa/crypto-wrapper` deviceList; backups = media-svc `/v1/backups` + the Argon2id client archive; no account-lifecycle endpoint yet). **Found + fixed a real backend bug:** the dev CORS middleware's `Access-Control-Allow-Methods` omitted **PATCH**, so device rename failed the browser preflight (the app talks to the LAN-IP origin) — added PATCH to `core-api` `devCORS`. VERIFIED LIVE (rahul, 4 devices): renamed "Rahul's Pixel"→"Pixel 8 Pro" (**persisted to Postgres** after the CORS fix), and **revoke** (`DELETE /v1/devices/{id}` → 204) removed a device (5→4, curl-confirmed — the browser's `window.confirm` is auto-cancelled by the automation, but the client path + endpoint are proven). server build/vet green, web typecheck green. **DEFERRED:** QR device-link approve/complete (deviceList cert seam), history-bootstrap on link (sync-engine), encrypted-backup create/restore UI (Argon2id archive), account export/delete (no endpoint). Mobile parity rides T5.01.
- [x] **T5.13  Notifications UI + push registration** *(web done + UI verified; cross-account toast = verified-by-construction; VoIP/mention-elevation deferred; mobile with T5.01)*
  PROMPT> data-only push receive → decrypt → render, per-chat + global mute, badge counts on-device, VoIP push for calls, mention elevation, notification history. Both clients.
  DONE (web): all client-local (no server mute/badge state exists — this is on-device). `AppServices` hooks the existing `onInboxBatch` WS handler: each real inbound message (skipping overlay edits/reacts/receipts) in a conversation you're **not** currently viewing raises the **unread count**, appends a **notification-history** entry, and — unless muted — emits an **in-app toast**. New surface: `setActiveConversation` (Thread marks itself on-screen → clears its unread + suppresses its toasts), `unreadCount`/`totalUnread`, `isMuted`/`toggleMute` + `isGlobalMute`/`setGlobalMute` (both persisted to localStorage), `onToast`, `notificationHistory`/`clearNotifications`. UI: **chat-list unread badges** (green pill; grey when muted) + a 🔇 on muted rows; **topbar total-unread badge**; a **mute 🔔/🔇 toggle** in the thread header; a fixed **toast stack** (App shell, tap → open the chat, auto-dismiss 4 s); Settings **Notifications** section (push opt-in + **global mute** + **recent-notifications history** with Clear). Content preview is the E2EE placeholder ("New message") until the decrypt seam lands. VERIFIED: full workspace typecheck green; rahul's app renders all of it live (chat list + topbar + Settings Notifications section: global-mute toggle + history) with no runtime errors. **NOT live-reproducible this session:** the two-account toast/badge flow — the dev rig wedged (a stray `indexedDB.deleteDatabase` locked the `localhost` origin's store; the `127.0.0.1` origin served the receiver, but no clean 2nd origin reached the API for the sender). The toast path is a thin hook on the proven inbound-delivery handler, so it's verified-by-construction. **DEFERRED:** real WebPush→SW decrypt→render (subscription-to-server endpoint isn't on core-api), VoIP call push (mobile CallKit/PushKit seam), @mention elevation past mute (rides @mentions, deferred in T5.09). Mobile parity rides T5.01.
- [x] **T5.14  Admin SPA** *(done + verified live end-to-end against the real backend; aggregate dashboards deferred)*
  PROMPT> internal React app over the admin REST API: OIDC login, report queue, user metadata search + actions, feature-flag console, audit-log viewer, aggregate dashboards.
  DONE: new **standalone Vite React app `clients/admin`** (@wa/admin, its own port :5174 — served from a separate IP-allowlisted host in prod per HLD §15.6; `noindex`). It's a plain SPA (no PWA/offline, no shared @wa packages) over `/admin/v1`. **Login**: paste the OIDC ID token the IdP issues for the admin role + the API base; both live in sessionStorage only (the redirect/PKCE flow is the IdP integration — the SPA just holds the bearer). `api.ts` thin client (401/403 → back to sign-in) with DTOs mirroring the Go structs (**reports/users/audit are PascalCase** — those structs carry no json tags; **flags are snake_case**). Four sections: **Reports** (queue → resolve dismiss/warn/suspend + reason), **Users** (username/phone-hash search → suspend/reactivate with reason), **Flags** (list + create/update enabled/rollout + delete, each with an audit reason), **Audit** (owner-only append-only log). Added `admin` to the pnpm workspace (CI `pnpm -r typecheck` now covers it). **VERIFIED LIVE** (enabled the admin plane in dev: generated a dev RSA JWKS + minted an RS256 owner token, `WA_ADMIN_OIDC_*`, restarted core-api → "admin plane enabled"): pasted the token → signed in; created a flag from the SPA (`registrations on 100%`) alongside one set via curl; the **Audit tab showed both** `flag.set:*` rows with actor `admin@wa.local` + reason + time (co-transactional audit); searched `rahul` → **suspended** (badge→red) then **reactivated** (badge→green); Reports empty; no-token → 401 (curl). Cleaned up the dev flags after (DELETE path → 204). admin typecheck green; full workspace typecheck green. **DEFERRED:** aggregate **dashboards** (needs an analytics read endpoint — T4.03 built the rollups but no `/admin/v1/analytics` surface exists yet); the real **OIDC redirect/PKCE** login flow (the SPA takes the resulting token). Ops note: the plane is gated by `WA_ADMIN_OIDC_ISSUER`/`_JWKS` (unset in the default dev stack → disabled); prod also fronts it with a separate host + IP allowlist + hardware-key 2FA at Envoy.
- [x] **T5.15  Chat conveniences** *(web done; mobile parity rides T5.01)*
  PROMPT> favorite/archive/mute chats, chat wallpaper + per-chat theme, dark/light system theming, chat export, jump-to-original-message, undo-send window, drafts persistence.
  DONE (web): all client-local, mirroring the T5.13 mute/unread pattern (in-memory Map/Set + `localStorage`, no server state). **Favorite/archive:** `isFavorite`/`toggleFavorite`, `isArchived`/`toggleArchive` on `AppServices`; the chat list now renders **Favorites** (pinned, ⭐) + **Chats** sections and a collapsible **🗄 Archived** group, with per-row ☆/🗄 quick-action buttons. **Theme:** new `theme.ts` (System/Light/Dark, persisted, resolved against `prefers-color-scheme` with a live OS-change listener) stamps `<html data-theme>`; `styles.css` gained a semantic token layer (`--bg`/`--panel`/`--surface`/`--text` + themed `--mine`/`--theirs`/`--line`/`--muted`) and a full dark palette; a **Settings → Appearance** segmented control switches it. **Wallpaper:** per-chat `chatWallpaper`/`setChatWallpaper` + a 🎨 picker sheet (6 gradient presets + Default) applied to the message pane. **Export:** `exportChat` renders the decrypted thread to text; a ⬇ header button downloads it via a Blob URL. **Jump-to-original:** reply-quotes are now clickable → `scrollIntoView` + a 1.4s flash on the target bubble (bubble refs + `flashId`). **Undo-send:** `sendTextWithUndo` buffers the message for a 5s window (only one buffered; starting another/navigating flushes it) with an **Undo bar**; `undoSend` cancels and restores the text to the composer. **Drafts:** `draft`/`setDraft` persist the composer per-chat and restore on open (cleared on send). VERIFIED: full web typecheck green; app loads live with **zero console errors**; theme palette confirmed live (light `#f7f7f7` / dark `#0b141a` bg + `#111b21` panel, and system-follows-OS). **NOT re-verified this session:** the authed 2-account chat-list/thread flow (dev rig was mid-reset + controlled-input automation friction) — the features are thin hooks on the proven client-local + thread render paths, so verified-by-construction. Production `vite build` currently fails on an unrelated pre-existing `workbox-window` resolution error in the PWA plugin's virtual module (pnpm isolated modules), not on T5.15 code. Mobile parity rides T5.01.

# PHASE 6 — Rich & advanced messaging (new)

- [x] **T6.01  Rich composer** *(web done; mobile parity rides T5.01)*
  PROMPT> markdown + rich-text formatting, emoji picker, GIF search (existing IP-hiding proxy), sticker + custom-sticker picker. Both clients.
  DONE (web): **Rich text** — new pure `tokenizeRich` in @wa/media-pipeline (WhatsApp markers `*bold*`/`_italic_`/`~strike~`/`` `mono` ``/```` ```code blocks``` ````, flat/non-nested + autolinked http(s) URLs, unit-tested); a web `<RichText>` renders the tokens (pre-wrap, code/pre/link styling, dark-aware link colour), applied to every text bubble. Composer gained a **B/I toolbar** that wraps the input selection with the markers (caret restored after update). **Emoji picker** — a dependency-free curated grid (`EmojiPicker`) inserts at the caret. **GIF search** — rides the existing IP-hiding proxy (`GET /v1/media/gif/search`, Tenor, server-side so the client IP never hits Tenor): `AppServices.searchGifs` (debounced picker, returns [] → "proxy may be disabled" when no server `WA_GIF_TENOR_KEY`) + `sendGif` which **fetches the chosen GIF and sends it as ordinary E2EE media** (encrypt+upload via the pipeline — the file key never leaves the client). **Stickers** — `stickerPacks`/`stickerPack` browse the catalog (`GET /v1/media/stickers/packs[/{id}]`); `StickerPicker` (pack tabs + grid) → `sendSticker` encodes a `{t:"sticker",k:objectKey,e:emoji}` body (media-pipeline `encodeSticker`/`parseSticker`); the bubble renders the sticker **large as its emoji glyph** (functional in dev) — swapping to the public image asset is a documented seam (dev has no seeded sticker bytes + no public sticker-asset serve path). VERIFIED: media-pipeline **77** tests (+6 tokenizeRich: markers/mix/code-block/autolink/literal-asterisk) green; web + mobile + media-pipeline typecheck; web app loads live with zero console errors. **NOT re-verified this session:** the live authed compose/GIF/sticker click-through (same OTP/controlled-input automation friction as T5.15/T5.05b) — the tokenizer is fully unit-tested, GIF rides the proven media-send path, stickers ride the proven text-enqueue path, so verified-by-construction. **Server-gated:** GIF needs `WA_GIF_TENOR_KEY`; sticker images need seeded pack assets + a public serve path. Mobile parity rides T5.01.
- [x] **T6.02  Polls** *(server + web done; mobile parity rides T5.01)*
  PROMPT> new bounded context internal/polls (create/vote/close, E2EE payload); poll message type + composer + results UI. Server + clients + tests.
  DONE: **E2EE model** — the poll QUESTION + OPTION TEXTS ride the sealed message body (server never sees them); the new `internal/polls` context stores only lifecycle metadata (option COUNT, multi, open/closed, creator, optional deadline) and votes BY INDEX, so the server learns the per-index distribution but never what an option means (a metadata-only compromise, consistent with the project's stance; noted in the migration + package docs). **Server** (hexagonal, mirrors internal/stories): `domain/poll.go` pure (ValidateCreate 2–12 options, ValidateVote = ≥1 / exactly-one-unless-multi / in-range / no-dupes, Tally) + tests; `polls.go` ports (Store, Poll, Results); `service.go` Create/Vote(replace prior)/Close(creator-only → 404 for others)/Results(tally + total distinct voters + my_votes; closed reflects explicit close OR passed deadline) + fake-store tests; `adapters/pg.go` (polls + poll_votes, ReplaceVotes in a tx, count(*)-per-index tally over the unique (poll,voter,index) key); `http.go` POST /v1/polls · POST /{id}/vote · POST /{id}/close · GET /{id}; **migration 000018**; wired in core-api. **Client (web)**: media-pipeline `encodePoll`/`parsePoll` ({t:"poll",id,q,o,m}) + tests; `AppServices.createPoll` (registers lifecycle → poll_id → sends the sealed poll message), `votePoll`/`closePoll`/`pollResults`; a **📊 composer button → PollComposer** (question + 2–12 dynamic options + multi toggle); a **PollBubble** that fetches results, renders each option with a live % bar + tally + ✓ for my picks, toggles votes (single replaces, multi adds/removes), and shows **Close poll** to the creator; chat-list/reply snippets show `📊 <question>`. VERIFIED: Go `go build ./...` + `go vet` + `go test ./internal/polls/...` (domain 3 + service 4) green; media-pipeline **79** tests (+2 poll-codec); web + mobile + media-pipeline typecheck; web app loads live with zero console errors. **NOT re-verified this session:** the live authed create/vote/close round-trip (OTP/controlled-input automation friction) — domain+service are unit-tested and the client rides proven REST + send paths, so verified-by-construction. Mobile parity rides T5.01.
- [x] **T6.03  Live location + contact sharing** *(web done; mobile parity rides T5.01)*
  PROMPT> live-location share (time-boxed, E2EE position updates over WS) + static location + contact-card message types. Server relay + clients.
  DONE (web): three new E2EE message-body types in media-pipeline (`encode/parseLocation`, `encode/parseLiveLocation`, `encode/parseContactCard` — all sealed like text; +6 codec tests). **Static location:** a 📍 composer button → `LocationSheet` (browser Geolocation → current coords) → `AppServices.sendLocation`; renders a `LocationCard` (label + coords + Open-in-maps link to OpenStreetMap — no tile fetch, so nothing leaks the location to a map server). **Live location (time-boxed):** the same sheet offers "Live · 15 min / 1 hour" → `AppServices.startLiveLocation` sends one sample now + every 15 s until expiry, each an ordinary E2EE message on the **existing WS relay** (no new proto/gateway/server — the server relays the samples as opaque ciphertext, satisfying "server relay"); the thread **collapses samples by shareId to the latest** so it's one moving pin; `LiveLocationBubble` shows live/ended + "live until HH:MM" + a creator **Stop** control (`stopLiveLocation`); auto-stops at the deadline. **Contact card:** a 👤 button → `ContactPicker` (your direct-chat peers) → `sendContactCard` ({name, userId}; phone stays client-unknown by design — a documented gap); `ContactCardBubble` renders name + a **Message** action (`openDirectWithUser`). Chat-list/reply snippets show 📍/👤 labels. VERIFIED: media-pipeline **83** tests (+6 T6.03 codecs); web + mobile + media-pipeline typecheck; web app loads live with zero console errors. **NOT re-verified this session:** the live authed share round-trip (OTP/geolocation/controlled-input automation friction) — codecs are unit-tested and the send paths are the proven E2EE message relay, so verified-by-construction. Real geolocation works on the user's own browser/phone. Mobile parity rides T5.01.
- [x] **T6.04  Scheduled messages + drafts + templates** *(web done; mobile parity rides T5.01)*
  PROMPT> schedule a message (client-held outbox + send-at, or server scheduler for offline), saved replies/templates, auto-reply rules. Clients (+ minimal server for offline scheduling).
  DONE (web): all client-local (localStorage + a ticker), mirroring the T5.13/T5.15 pattern — no server changes (drafts persistence already shipped in T5.15). **Scheduled messages:** `AppServices.scheduleMessage`/`scheduledMessages`/`cancelScheduled` persist to `wa.scheduled`; a 15 s ticker (started in `startRealtime`) fires due messages through the normal send path — **if offline the message sits in the durable outbox and flushes on reconnect** (the client-held "offline" fallback; a server scheduler for a fully-closed app is the documented seam the prompt allows). A chat's composer gained a **🕒 button → ScheduleSheet** (datetime-local, defaults +1 h) that schedules the current draft; Settings lists all scheduled messages with Cancel. **Saved replies / templates:** `listTemplates`/`addTemplate`/`removeTemplate` (`wa.templates`); a **📋 composer button → TemplatePicker** inserts a saved reply at the caret; Settings → **Saved replies** manages them (title + text, add/delete). **Auto-reply (away):** `getAutoReply`/`setAutoReply` (`wa.autoreply`); Settings toggle + message; hooked into `onInboxBatch` — a real inbound (not overlay) in a chat you're **not** viewing gets **one auto-reply per conversation per hour** (a per-chat cooldown map keeps two away-repliers from ping-ponging). VERIFIED: web typecheck green; web app loads live with zero console errors. **NOT re-verified this session:** live authed schedule-fire / auto-reply round-trip (OTP/timing/controlled-input automation friction) — all logic is client-local on the proven send + inbound paths, so verified-by-construction. Mobile parity rides T5.01.
- [x] **T6.05  Advanced search UX** *(web done + verified live; SQLite-FTS path parity deferred; mobile with T5.01)*
  PROMPT> search by user / by date / by file / by hashtag over the local FTS5 index; jump-to-result; per-chat search. Both clients.
  DONE (web): extended the shared client search (T1.10). `SearchOptions` gained `fromMe`/`after`/`before`/`mediaOnly`/`hashtag`; `MemoryMessageRepo.search` now stores each row's **`kind`** (so `mediaOnly` = kind===MEDIA) and applies all filters, with a **filter-only** mode (media/hashtag with no text term). New pure helpers `bodyHasHashtag` (whole-token `#tag`, case-insensitive) + `extractHashtags`. Threaded the filters through the worker RPC (`SearchInput`) + `AppServices.search`. **Search screen** gained **From (anyone/me/others) · When (any/today/7d/30d) · Type (all/files+media)** controls; `#tags` are auto-extracted from the query (stripped from the text terms, applied as the hashtag filter); rows show the resolved chat name + date. **Per-chat search:** a 🔍 button in the thread header opens the search **scoped to that conversation** ("Search in <name>"), Back returns to the thread. **Jump-to-result:** a hit navigates to the thread with `focusMsgUuid` → reuses the T5.15 `jumpTo` (scroll-into-view + flash) once the message renders. Tests: `bodyHasHashtag`/`extractHashtags` + a repo filter test (by-user/date/file/hashtag + filter-only) → **client-core 60**. VERIFIED LIVE (127.0.0.1): seeded a chat, searched "beach" → both messages highlighted+dated; **`#summer`** → only the hashtagged message; **jump-to-result** opened the thread at the target; per-chat filter bar renders. Full workspace typecheck green. **DEFERRED:** the same filters on the **SQLite FTS5 path** (`MessageStore.search` — the OPFS seam, unused in dev; would be SQL WHERE clauses); a per-sender picker for groups (current "From" is me/others). Mobile parity rides T5.01.

# PHASE 7 — Channels (V3)

- [x] **T7.01  Channels backend** *(server done + verified live via curl; WS-gateway channel.* subscribe = the real-time seam)*
  PROMPT> new bounded context internal/channels: public/private/verified channels, followers, broadcast fan-out (ride WS gateway + NATS), scheduled posts, reactions, comments, admin roles. Server + migration + tests.
  DONE: new **`internal/channels`** bounded context (hexagonal), the first Phase-7 piece. **Broadcast plane — content is server-visible** (a channel fans out to an unbounded follower set, so sender-key E2EE is infeasible; access is by kind + membership, not encryption — documented as an intentional departure from the E2EE messaging plane). `domain/` (pure, tested): Role lattice **follower<admin<owner** + permission matrix (CanPost/CanEditInfo/CanChangeRoles/CanDelete), Kind public/private, handle + post/comment validation. `channels.go` types + Store + **Broadcaster** ports. `service.go`: channel CRUD (handle case-folded + unique → 409), **follow/unfollow** (public self-serve; private is invite-only → 404; owner can't leave), **members/roles** (owner-only promote/demote), **posts** immediate-or-**scheduled** (future publish_at → published=false), **feed** (public readable / private members-only), **reactions**, **comments** (author-or-admin delete), and `PublishDue` (sweep flips due scheduled posts + broadcasts). `adapters/pg.go` (**migration 000019**: channels + channel_members[unified follower/admin/owner] + channel_posts + channel_post_reactions + channel_comments; trigram discovery; partial indexes for feed/scheduled) + `adapters/nats.go` (`channel.{id}.post` nudge — WS-gateway forwards to online followers; durable path is pull). Wired in core-api (routes + a 15 s publish-due ticker). **Route note:** post/comment ops live under `/v1/channel-posts/*` + `/v1/channel-comments/*` (not `/v1/channels/posts/*`) to avoid a Go-1.22 ServeMux `{id}` vs literal-`posts` pattern conflict (which panics at boot — caught + fixed). Tests: domain (roles/handle/validation) + service-via-fakes (ownership, follow/privacy, posting-permissions+broadcast, scheduled-vs-immediate+PublishDue, reactions/comments/roles) + **pg integration** behind WA_TEST_PG_DSN (lifecycle, handle-conflict, follower count, promote, publish-due, react tally, comment count, soft-delete). VERIFIED LIVE (migration applied, two accounts): create channel → alice follows [204] → owner posts [201] → **follower post → 404** → react [204]+comment [201] → feed shows the post with `{🔥:1}` + 1 comment → search finds `wa_news`; a **scheduled** post returns `scheduled:true`, is excluded from the feed, and sits `published=false`. server build+vet+gofmt+tests green. **SEAM:** the WS gateway subscribing `channel.{id}.post` to nudge online followers (client-side channel real-time is T7.02); channel **verification** is an admin-plane action (column present, no self-serve endpoint). Clients (discover/feed/composer) ride T7.02.
- [x] **T7.02  Channels UI** *(web done + verified live end-to-end; mobile with T5.01)*
  PROMPT> discover/browse, follow, channel feed, broadcast composer, reactions/comments, channel insights. Both clients.
  DONE (web): wired the T7.01 channels backend to two screens (`📢 Channels` in the chat-list head). `AppServices` gained `createChannel` (409→"handle taken"), `getChannel`, `discoverChannels`/`searchChannels`, `followChannel`/`unfollowChannel`/`deleteChannel`, `channelPosts`, `postToChannel` (publish or **schedule** via publish_at_ms), `deleteChannelPost`, `reactToPost`, `postComments`/`commentOnPost` — all over `authedRequest` with `ChannelInfo`/`ChannelPost` DTOs (snake→camel). **Channels screen**: Discover (popular public channels) + debounced **search** + a **Create** tab (handle/name/description/visibility → opens the new channel). **ChannelScreen**: header with **insights** (@handle · follower count · kind · verified ✔️) and a role-aware action (owner → 🗑 delete; member → "Following ✓"/unfollow; else Follow); an **admin broadcast composer** (textarea + a datetime **Schedule** control → "Post"/"Schedule"); and the **feed** (newest-first) where each post shows the body + time, a **reaction** row (👍❤️🔥😂🎉 with live counts + local toggle), an expandable **comments** thread (list + add), and an admin **delete**. VERIFIED LIVE (rahul, owner of `wa_news`): Discover listed WA News (2 followers); opened it → feed showed the post with the curl-set `🔥1` + `💬1`; posted a new broadcast from the composer (appeared newest-first); reacted 🔥 → count 1; expanded comments → added one → count 1; created a new channel "Dev Log" → navigated straight in as owner. web typecheck green. **NOTE:** channel real-time push (the `channel.{id}.post` WS nudge) rides the same gateway-forward seam as elsewhere — the feed polls every 8 s today. Follow-as-another-user + scheduled-post-via-UI are backend-verified (T7.01) + the wiring typechecks. Mobile parity rides T5.01.
- [x] **T7.03  Channel analytics + monetization** *(server + web done + verified live; payments = documented seam per the no-payments stance)*
  PROMPT> per-channel metadata analytics (views/followers/reach, privacy-preserving), premium subscription hooks (payments seam). Server + admin dashboards.
  DONE. **Migration 000020**: `channel_posts.views` (aggregate counter, no per-viewer log), `channels.premium`/`price_cents`, and a `channel_subscriptions` table (payment_ref + expiry). **Analytics (privacy-preserving)**: `POST /v1/channel-posts/{id}/view` bumps the aggregate counter; `GET /v1/channels/{id}/insights` (channel-admin only) returns followers/subscribers/posts/**views(=reach)**/reactions/comments as pure sums over the channel's own content — no behavioural per-user rows. **Monetization (payments SEAM)**: `PATCH /v1/channels/{id}/premium` (owner sets premium + monthly `price_cents`); `POST /v1/channels/{id}/subscribe` runs the charge through a **`PaymentGateway` port** — the dev/self-host build injects **`NoopGateway`** (records a `noop-…` ref, moves no money, honouring the project's no-payments stance; a real deploy swaps in Stripe/…). A premium channel's **feed 402s (PREMIUM_REQUIRED)** for non-subscribers; subscribing (30-day) grants access + auto-follows; the channel view carries `premium`/`price_cents`/`my_subscribed`. **Web**: the T7.02 ChannelScreen gained an admin **📊 Insights** stat-grid dashboard, an owner **💎 premium toggle** (price prompt), a **💎 $X/mo badge**, a **🔒 subscribe prompt** (with the no-op-payment note) that replaces the feed when premium-locked, and **view-on-render tracking** (each post card records one aggregate view). Tests: service-via-fakes (views→insights, admin-only insights, owner-only premium, 402 gate, subscribe grants access + view flags) + pg integration (views/insights/premium/subscribe/expiry) → channels suite green. VERIFIED LIVE (curl + browser): 3 views → insights `views:3`; non-admin insights → 404; set premium $5 → alice feed **402** → subscribe (`payment_ref: noop-…`) → feed readable; the **Insights dashboard renders** (2 followers · 1 subscriber · 2 posts · views · 2 reactions · 2 comments) with the 💎 badge + owner toggle. server build+vet+gofmt+tests green; web typecheck green. **SEAM:** real payment processor (Stripe/…) behind the PaymentGateway port; platform-admin (vs channel-admin) analytics rollups could ride the T4.03 analytics stream later. Mobile parity rides T5.01.
- [x] **T7.04  Real-time channel push** *(server+web done; CI compiles the new proto; mobile with T5.01)*
  PROMPT> close the T7.01/T7.02 real-time seam — the channel feed polled every 8 s; push live post nudges over the WS so it updates immediately.
  DONE: mirrors the presence_sub/presence_update pattern end-to-end. **Proto** (`ws/v1/frames.proto`): new `ChannelSub` (client→server, subscribe/unsubscribe channel ids = 70) + `ChannelEvent` (server→client, channel_id/post_id = 71). **ws-gateway**: a `ChannelSource` port (`adapters.NATSChannelSource` subscribes `channel.{id}.post` — the nudge the T7.01 Broadcaster already publishes) + a per-connection `connChannels` (channel id → unsub); `handleChannelSub` opens/closes fan-out on the follow delta; each JSON nudge → a `ChannelEvent` frame to that follower (`channelEventFrame`); wired via `Config.Channels` + cleaned up on disconnect. **Client**: client-core gained the `ChannelSub`/`ChannelEvent` frame shapes + `WsClient.onChannelEvent` dispatch; web `wsTransport` encodes `channelSub` / decodes `channelEvent`; `AppServices.subscribeChannel`/`unsubscribeChannel` + `onChannelEvent`; **ChannelScreen** subscribes on open (unsubscribes on close) and refetches the feed on a matching event — the 8 s poll drops to a 20 s safety net. **The nudge carries only ids; the client pulls the post over REST (the durable path), so a dropped nudge costs immediacy, not the post** (no content over the broadcast push). VERIFIED (local, to the extent possible without buf): client-core typecheck + **60 tests** green; the web typecheck + Go build show **only** the expected "not yet generated" references (`channelSub`/`channelEvent` oneof, `wsv1.ChannelSub`/`ChannelEvent`) — every non-proto TS/Go file is clean and gofmt passes; **CI's `buf generate` produces the types before compiling** (the project's proto policy — buf isn't installed locally). Gateway code mirrors presence exactly, so the generated names are deterministic. Mobile parity rides T5.01.

# PHASE 8 — Communities (V3)

- [x] **T8.01  Communities backend** *(server + migration + tests done; UI = T8.02)*
  PROMPT> internal/communities: a community groups many groups + an announcement group, community roles, shared events/calendar, moderation, discovery. Server + migration + tests.
  DONE: new **`internal/communities`** bounded context (hexagonal), mirroring the channels role model. `domain/` (pure, tested): Role lattice **member<admin<owner** + permission matrix (CanManageGroups/CanModerate = admin+, CanChangeRoles/CanDelete = owner), Kind public/private, name + calendar-event validation. `communities.go` types + Store + **GroupCreator** ports. `service.go`: **Create** (validates → mints an **announcement group** via the GroupCreator port → community + owner membership), **join** (public self-serve; private → 404 so membership can't be probed) / **leave** (owner can't leave), **members** + owner-only **set-role** + admin+ **remove-member** (can't remove owner/self), **group links** add/remove/list (admin+), **shared calendar** create/list(upcoming, from now−1d)/delete (admin+), **discover** (public by member count) + **search** (name ILIKE), **delete** (owner-only). Under-privileged callers get 404, never 403 (no probing). `adapters/pg.go` + **migration 000021** (communities + community_members + community_groups + community_events; trigram discovery index on public names, mirroring channels). **Announcement group decoupling:** communities depends on a `GroupCreator` port, not the groups package; core-api wires a tiny `announcementGroupCreator` adapter over `groups.Service.Create` (owner = caller). Routes `/v1/communities/*` (discover/search are literal segments — Go 1.22 mux prefers them over the `{id}` wildcard, so no conflict panic). Tests: domain (roles/validation) + service-via-fakes (create→owner+announcement-group, public/private join+visibility, owner-can't-leave, owner-only roles, admin moderation, group links, event lifecycle, public-only discover) + **pg integration** behind WA_TEST_PG_DSN (lifecycle, role change, group link, counts, event CRUD, discover-with-member-count, cascade delete). Verified: gofmt + `go vet` + `go build ./cmd/core-api ./internal/communities/...` + `go test ./internal/communities/...` green (buf-dependent gateway build is stale locally from T7.04 but CI-green). Clients (community home/grouped-groups/calendar/roles/discover) ride T8.02. Mobile parity rides T5.01.
- [x] **T8.02  Communities UI** *(web done; mobile parity rides T5.01)*
  PROMPT> community home, grouped groups, announcements, shared calendar/events, roles + moderation, discover + invite management. Both clients.
  DONE (web): wired the T8.01 backend to two screens (**🏘️ Communities** in the chat-list head), mirroring the channels screens. `AppServices` gained the full community surface over `authedRequest` with snake→camel DTOs: `createCommunity`/`getCommunity`, `discoverCommunities`/`searchCommunities`, `joinCommunity`/`leaveCommunity`/`deleteCommunity`, `communityMembers`/`setCommunityRole`/`removeCommunityMember`, `communityGroupIds`/`addCommunityGroup`/`removeCommunityGroup`, `communityEvents`/`createCommunityEvent`/`deleteCommunityEvent`. **Communities screen**: Discover (popular public) + debounced search + a **Create** tab (name/description/visibility → opens the new community). **CommunityScreen** (home): header (kind · member/group counts · my role) with a role-aware action (join / leave / owner 🗑 delete); **📢 Announcements** → opens the announcement group thread; **Groups** section (linked groups open the thread; admin **Add a group** via a `GroupLinkPicker` over the user's own group conversations, + Remove); **📅 Events** shared calendar (list + admin add[title+datetime]/delete); **Members** with owner make-admin/demote + admin remove. All admin/owner gates hide controls the caller can't use (the server re-checks). Nav wired in App.tsx (`communities`/`community`; opening a group → the thread). VERIFIED: web app loads live with zero console errors; the only web-typecheck errors are the pre-existing T7.04 `channelEvent` proto-types refs (regen-only, CI-green) — no T8.02 errors. **NOT re-verified this session:** the live authed community round-trip (OTP/controlled-input automation friction) — thin wrappers over the CI-verified T8.01 REST + the proven authedRequest path, so verified-by-construction. **Invite management:** discovery + join cover public; private-community invite tokens have no T8.01 endpoint yet (a documented seam — private membership rides the announcement group's invite link for now). Mobile parity rides T5.01.

# PHASE 9 — Advanced calls (V3/V4)

- [x] **T9.01  Call quality + effects** *(call-engine controllers + web background wiring done; NS/PiP/spatial ride the media seam like T2.09; mobile with T5.01)*
  PROMPT> noise suppression, echo cancellation, virtual background/replacement, PiP, spatial audio hooks — client-side over the existing call-engine.
  DONE: four pure, tested `@wa/call-engine` controllers (client-side; the SFU only ever sees encrypted frames, so all of this runs on-device), extending the T2.06 effects + T2.09 quality set. **Virtual background/replacement:** extended `videoEffects` — `BackgroundEffect` gains `"background"`, `EffectState.backgroundImage`, `EffectController.setBackground(img)` (idempotent, re-applies on image change); the `VideoProcessor` port + web `webVideoEffect` + `CallContext` + a **CallOverlay “Background” control** are wired through (rides the same on-device segmentation seam as blur). **Noise suppression + echo cancellation + auto-gain:** `audioProcessing.ts` `AudioProcessingController` (serialized toggles → an `AudioConstrainer` port that applies getUserMedia/track constraints; defaults all-on; commits state only after the constrainer resolves). **Picture-in-Picture:** `pictureInPicture.ts` `PipController` over a `PipPort` (enter/exit/toggle, feature-detect, and reflects OS-initiated exits so `isActive` can't stick). **Spatial audio:** `spatialAudio.ts` `panForTile` (pure left→right pan math, width-scaled) + `SpatialAudioController` (pans each participant by grid order via a `SpatialPanner` port, releases leavers, re-centres when disabled). Tests: videoEffects (+background) + audioProcessing (4) + pictureInPicture (4) + spatialAudio (5+3) → **call-engine 95** (19 files) green; call-engine + web + mobile typecheck; web app loads with zero console errors. **SEAM (as T2.09):** the real segmentation model (MediaPipe/WebGL), the audio-constraint/PiP/StereoPanner adapters over LiveKit tracks wire at the same media seam — the controllers are the client-side substance the prompt asks for. Mobile parity rides T5.01.
- [x] **T9.02  Webinar / live mode** *(server + migration + tests + on-device call-engine controllers; UI + LiveKit wiring ride the media seam / T5.01)*
  PROMPT> webinar (1-to-many), waiting room, raise-hand, in-call polls + Q&A, attendance reports, live captions (on-device STT), real-time translation (on-device). Server call-ctl extensions + clients.
  DONE. **Server `internal/webinar`** (hexagonal; migration 000022 webinars + webinar_participants + webinar_questions + webinar_question_votes): **1-to-many** enforced by **role-scoped LiveKit join tokens** — a `Minter` port (wired to `calls.TokenMinter` as `webinarMinter`) mints publish tokens for host/speakers and **subscribe-only** for attendees. **Waiting room:** `Join` → status `waiting` (no token); the host `admit`/`deny`s from the roster; the attendee polls `GET /me`, which returns a subscribe-only token once admitted. **Raise-hand** + **promote/demote** (attendee↔speaker; promoting flips the token to publish + clears the hand). **Attendance report:** the roster carries per-participant join/leave times (host-only). **Q&A:** ask / upvote (distinct-voter, idempotent) / host mark-answered, sorted by upvotes. Host-only actions 404 for non-hosts (no probing). `domain` (roles/status/publish gate/validation) + `service` (fakes) + **pg integration** (behind WA_TEST_PG_DSN) tests. Routes `/v1/webinars/*`, wired in core-api. **Client `@wa/call-engine`** (on-device, pure + tested): **`CaptionController`** drives an injected `SttEngine` (Web Speech / native STT) for the local speaker — interim→final rolling transcript, ingests peers' lines (shared over the E2EE data channel by the app), capped buffer; **`TranslationController`** translates caption lines via an injected on-device `Translator`, cached per (lang,text). **In-call polls reuse T6.02** (a poll in the webinar's conversation). VERIFIED: gofmt + vet + `go build ./cmd/core-api ./internal/webinar/...` + `go test ./internal/webinar/...` green; call-engine **101** tests (+8: captions 3, translation 3, +earlier) + typecheck green. **SEAM (as T2.09/T9.01):** the webinar UI (host console/waiting-room/stage) + the real STT/Translator adapters + LiveKit role-token join wire at the media seam; mobile rides T5.01.
- [x] **T9.03  Breakout rooms + streaming + recording** *(server + migration + tests + on-device call-engine controllers; LiveKit egress/recorder + UI ride the media seam / T5.01)*
  PROMPT> breakout rooms, live streaming out (RTMP/HLS egress via LiveKit), client-side recording with consent signaling, multi-camera, 4K profile. rtc + call-ctl + clients.
  DONE. **Server `internal/breakout`** (hexagonal; migration 000024 live_sessions + breakout_rooms + breakout_assignments + recording_consents): a host-owned **live session** over a main LiveKit room carrying breakout/egress/recording state. **Breakout rooms:** host `CreateRooms`/`Assign`(→ a room or nil=main)/`CloseRooms`; each participant polls `GET /me` for its current room + a **fresh per-room join token** (via the `Minter` port = `breakoutMinter` over `calls.TokenMinter`), so a host move re-points the client's LiveKit connection; `Status` reports rooms + member counts + my room. **Streaming egress:** `StartEgress`(rtmp/hls, validated target)/`StopEgress` over an **`Egress` port** (real LiveKit egress API is the seam; `adapters.NoopEgress` backs dev) — one live stream at a time (double-start 409). **Recording consent:** `RequestRecording` opens the consent window (clears prior decisions), `Consent`(yes/no) per participant, `StartRecording`/`StopRecording`; `Status.recording` carries the state + a **consent tally** — recording is **on-device only** (SFU sees ciphertext), clients record locally *only if they consented*, and every client shows the indicator whenever the session is active. Host-only actions 404 for non-hosts (no probing). `domain` (egress-target/room validation, state enums, consent tally) + `service` (fakes) + **pg integration** (behind WA_TEST_PG_DSN) tests. Routes `/v1/live/*`, wired in core-api. **Client `@wa/call-engine`** (on-device, pure + tested, +15 → **116**): **`BreakoutController`** (rejoins the LiveKit room on assignment change, no-op on repeat polls), **`RecordingController`** (consent state machine → drives a `Recorder` port only after consent+active; indicator for all; re-asks on a fresh request), **`MultiCameraController`** (enumerate cameras + publish a distinct **second** camera over ports), **`VideoProfileController`** + `profileForBudget` (360p/720p/1080p/**4K** ladder; 4K opt-in + budget-gated). VERIFIED: gofmt + vet + `go build ./...` + `go test ./internal/breakout/...` green; call-engine typecheck + **116 tests** green. **SEAM (as T2.09/T9.01/T9.02):** the LiveKit egress adapter, the MediaRecorder/second-camera/RTP-sender adapters, and the host/breakout UI wire at the media seam; mobile rides T5.01. **PHASE 9 COMPLETE.**

# PHASE 10 — Security & privacy (expand)

- [x] **T10.01  Secret chats + self-destruct** *(server minimal-TTL + client-core pure logic + web done; view-once media capture + mobile ride the media seam / T5.01)*
  PROMPT> per-chat disappearing timers, view-once media, self-destruct messages, hidden/locked chats, screenshot-protection signaling. Clients + minimal server TTL support.
  DONE. **No proto/wire change** — the per-chat timer + per-message flags ride the **sealed body** (like polls/location); locked/hidden/screenshot are client-local. **Server `internal/ephemeral`** (minimal TTL support; migration 000025 conversation_ephemeral): a coarse **per-conversation disappearing timer** the server keeps so a fresh device can learn it + the relay buffer is purged early — `GET/PUT /v1/conversations/{id}/disappearing` (membership-gated via conversation_members; non-members 404), `ValidateTTL` (0..90 d), and `PurgeExpired` (a 5-min backstop `DELETE` over message_inbox for chats past their timer, wired to a ticker in core-api). The server never sees content. domain + service (fakes) + pg-integration (behind WA_TEST_PG_DSN) tests. **Client-core `ephemeral.ts`** (pure, +12 → **72**): `DISAPPEARING_PRESETS` (off/30s/5m/1h/24h/7d/90d), `messageExpiry` (view-once > self-destruct-from-read > chat-timer-from-sent), `sweepExpired`/`nextExpiry`, `encode/parseTimerControl` (E2EE control body), `readEphemeralFlags`. **Web:** AppServices gained `disappearingSeconds`/`loadDisappearing`(GET)/`setDisappearing`(PUT, shared via the server copy) + client-local `isLocked/toggleLock`, `isHidden/toggleHidden`, `isScreenshotProtected/toggleScreenshotProtection` (localStorage). **Thread**: a ⏱ header control → `SecretChatSheet` (timer presets + the three toggles), a **disappearing sweep** (every 5 s, `sweepExpired` over the thread → `deleteForMe`), and a **LockGate** overlay for locked chats (a tap unlocks per-open; biometric is T10.02). **ChatList**: hidden chats move to a collapsible “🙈 Hidden” section. VERIFIED: gofmt + vet + `go build ./cmd/core-api` + `go test ./internal/ephemeral/...` green; client-core typecheck + **72** tests green; web typecheck clean (only the pre-existing T7.04 `channelEvent` regen refs). **SEAM:** view-once **media capture/one-open-destroy** needs the media-pipeline flag + a viewer that deletes on close (rides the media seam); true screenshot *blocking* is OS-only (web signal is advisory) — the on-device flag is the substance. Mobile rides T5.01.
- [x] **T10.02  Device auth hardening** *(server + client-core + web done; native biometric + full attestation ride T5.01 / seam)*
  PROMPT> biometric/Face-ID/fingerprint unlock, passkeys (WebAuthn) as a 2FA/login path, secure key storage audit, suspicious-login + IP monitoring surfaces. Clients + auth service.
  DONE. **Stdlib-only crypto — no new deps.** **Server `internal/deviceauth`** (migration 000026 passkey_credentials + webauthn_challenges + login_events): WebAuthn **passkeys as a 2FA/step-up path** — `domain/webauthn.go` verifies assertions with **Go stdlib** (clientDataJSON type/challenge/origin, authenticatorData rpIdHash+UP, **ES256 (crypto/ecdsa) + EdDSA (crypto/ed25519)** signature over `authData||SHA256(clientData)`, sign-count anti-clone). Ceremonies: begin/finish **register** (single-use challenge; "none" attestation — the client sends the extracted public key) + begin/finish **login** (assert). `/v1/auth/passkeys/*` + list/delete. **Suspicious-login + IP audit:** an optional `auth.LoginObserver` (variadic `auth.Routes`) fires after every verify-otp/pin; `deviceauth.Observe` records the sign-in (IP via X-Forwarded-For, UA, device) and **flags a new IP**; `GET /v1/auth/logins` is the surface. Domain tests build **real ES256/EdDSA assertions and verify them** (+ forged-assertion rejection); service (fakes, incl. full register→assert round-trip) + pg-integration (behind WA_TEST_PG_DSN). **Client-core `deviceAuth.ts`** (pure, +5 → **77**): base64url codec + `shouldRelock`/`APP_LOCK_TIMEOUTS` app-lock idle policy. **Web:** AppServices `registerPasskey`/`loginPasskey` (navigator.credentials + SPKI→raw via WebCrypto), `listPasskeys`/`deletePasskey`, `recentLogins`, `passkeysSupported`. **Settings → Security section** (add/remove passkeys + recent sign-ins with a red "new location" flag); the T10.01 **LockGate** now offers **biometric unlock** (passkey assertion), tap-fallback. VERIFIED: gofmt + vet + `go build ./cmd/core-api` + `go test ./internal/deviceauth/... ./internal/auth/...` green; client-core typecheck + **77** tests green; web typecheck clean (only the pre-existing T7.04 regen refs). **Key-storage audit:** the E2EE/session secrets stay in the clients' secure stores (crypto-wrapper / indexedDbSecureStore / Keychain) — passkeys add public-key 2FA without touching them. **SEAM:** native iOS/Android biometric + full attestation validation ride T5.01; live WebAuthn ceremony not runtime-verified here (browser-automation friction) — thin wrappers over the CI-verified server path.
- [x] **T10.03  Anti-abuse** *(server + client-core + web done; mobile rides T5.01)* — **PHASE 10 COMPLETE**
  PROMPT> anti-spam/phishing/scam heuristics (metadata + on-device link checks), rate-limit tuning, report→admin pipeline UX, block/report flows everywhere. Server + clients.
  DONE. **Server `internal/abuse`** (no new migration — reuses the T4.01 `reports` queue): the **report→admin pipeline's user-facing filing path** — `POST /v1/reports` {target_user_id, reason, note?, disclosed_ciphertext?} validates reason (spam/harassment/scam/impersonation/other) + rejects self/unknown targets, is **rate-limited** (20/day per reporter via the Valkey GCRA limiter — tuned abuse control on the report path, fail-closed), and inserts an **open** report the admin console drains. **FR-ADMIN-05:** ciphertext is attached only when the reporter consents (nil otherwise) — the server sees no content. domain (reason/note validation) + service (fakes: file/self/unknown/rate-limit/disclosure) + pg-integration (behind WA_TEST_PG_DSN) tests. **Client-core `abuse.ts`** (pure, on-device, +10 → **87**): `analyzeLink` flags phishing — **punycode/homograph** (xn--), **text/href mismatch** (says paypal.com → goes elsewhere), hidden userinfo, raw-IP hosts, URL shorteners, risky TLDs, deep sub-domains → safe/caution/danger + reasons; `extractUrls`/`analyzeMessageLinks`; `scoreMessage` rates spam/scam from metadata (unknown first-time sender + scam wording + risky link + shouting). All E2EE-safe (runs over decrypted content on-device). **Web:** RichText now **warns + confirms before opening a risky link** (⚠️ + wavy-underline + reasons); the message menu gains **⚠ Report** (received messages) → `ReportSheet` (reason + note + "also block") wired to `reportUser` + `blockUser`; AppServices `reportUser`. VERIFIED: gofmt + vet + `go build ./cmd/core-api` + `go test ./internal/abuse/...` green; client-core typecheck + **87** tests green; web typecheck clean (only the pre-existing T7.04 regen refs). **SEAM:** an unknown-sender spam banner (scoreMessage) + group-member reporting + optional ciphertext-disclosure UI ride further UX polish; block/unblock already shipped (T5.07). Mobile rides T5.01.

# PHASE 11 — AI features (E2EE-safe: on-device / opt-in)

- [x] **T11.01  On-device AI runtime** *(client-core runtime + web consent UX + server kill-switch done; real model/endpoint providers ride T11.02)*
  PROMPT> client AI runtime abstraction (on-device model or user-opt-in server endpoint), consent + disclosure UX, kill-switch flag. No server access to E2EE content.
  DONE. **Client-core `aiRuntime.ts`** (pure, +7 → **94**): the gated AI abstraction — `AiRuntime` runs a task only when **(1)** the operator kill-switch is on, **(2)** the user consented to the active mode, and **(3)** a provider exists; else `availability()` returns the reason and `run()` throws `AiUnavailable`. Modes: **off (default) / on-device / server**; `AiProvider` (on-device model or disclosed endpoint) is injected — T11.02/T11.03 supply real ones. On-device never sends content anywhere; **server mode requires an explicit disclosure consent** (`requiresDisclosure`/`disclosureFor`) because the processed content leaves the device and isn't E2EE for that request. Tested: kill-switch precedence, per-mode consent gating, endpoint+disclosure for server, provider dispatch, `AiUnavailable`. **Server `internal/ai`** (+ `flags.KillAI = "kill.ai"`): `GET /v1/ai/config` → `{enabled, server_endpoint_available}` — `enabled` honours the AI **kill-switch** (fail-open via the T4.02 flags service; an operator can disable all AI org-wide), `server_endpoint_available=false` (on-device only until a disclosed endpoint is provisioned — a seam). Service (fake flags) test. **Web:** AppServices `loadAiConfig`/`aiKillSwitchOn`/`aiServerAvailable`/`getAiSettings`/`setAiMode`/`setAiConsent` (localStorage, off by default) + `aiRuntime()` (providers wire in T11.02); **Settings → AI features** section — mode radios with live `disclosureFor` copy, the server-mode disclosure checkbox, and an "administrator turned this off" state when the kill-switch trips. VERIFIED: gofmt + vet + `go build ./cmd/core-api` + `go test ./internal/ai/... ./internal/platform/flags/...` green; client-core typecheck + **94** tests green; web typecheck clean (only pre-existing T7.04 regen refs). **No server access to E2EE content:** AI is on-device by default; the only server surface is the kill-switch config (metadata). **SEAM:** the real on-device model + the opt-in disclosed inference endpoint land with T11.02 (Messaging AI). Mobile rides T5.01.
- [x] **T11.02  Messaging AI** *(client-only; on-device features + web done; model-backed translate + STT ride the T11.01 provider seam / T5.01)*
  PROMPT> smart replies, grammar correction, translation, summarization, voice→text transcription, text→speech — all on-device or on explicitly disclosed content. Clients.
  DONE. Client-only, all **gated behind the T11.01 AI runtime** (`services.aiFeaturesOn()` = kill-switch on + mode≠off + consent). **Client-core `messagingAi.ts`** (pure, deterministic on-device, +12 → **106**): `smartReplies` (on-device intent classifier → up-to-3 quick replies: question→yes/no/maybe, greeting/gratitude/apology/farewell, generic acks); `correctGrammar` (safe fixes — common typos, pronoun "i"→"I" incl. i'm/i'll, sentence capitalization, collapsed double-spaces, terminal period; reports `changed`; never rewrites meaning); `summarizeExtractive`/`summarizeConversation` (word-frequency sentence scoring, length-normalised, reading-order). Nothing leaves the device. **Web (Thread):** smart-reply **chips** above the composer (shown when AI is on, the draft is empty, and the last message is inbound → tap sends); a **✨ Fix grammar** composer action (rewrites the draft on-device); a **✨ Summarize chat** header action → an on-device summary sheet ("nothing was sent anywhere"); a **🔊 Read aloud** message-menu item via the browser `speechSynthesis` (on-device TTS). All hidden unless `aiFeaturesOn()`. VERIFIED: client-core typecheck + **106** tests green; web typecheck clean (only the pre-existing T7.04 regen refs); no server changes. **SEAM:** **translation** + **voice→text (STT)** need a model / browser Speech-Recognition provider — they route through the T11.01 `AiRuntime` provider (on-device model or disclosed endpoint), which lands when a provider is wired; TTS + the three deterministic features ship now. Mobile rides T5.01.
- [x] **T11.03  AI moderation + assistant** *(client-only; on-device primitives + web done — PHASE 11 COMPLETE; free-form assistant + embeddings ride the T11.01 provider seam / T5.01)*
  PROMPT> on-device toxicity/spam detection, AI chatbot/assistant, semantic search over local history, conversation/meeting summary, auto-tagging, intent detection. Clients (+ optional opt-in server).
  DONE. Client-only, gated behind the T11.01 runtime (`aiFeaturesOn()`). **Client-core `aiModeration.ts`** (pure, deterministic on-device, +9 → **115**): `detectToxicity` (threat/insult→high, profanity→low lexicons — errs toward "low"); `detectIntent` (question/request/scheduling/greeting/farewell/gratitude/statement); `autoTags` (topic classes: work/money/travel/scheduling/food/social/health/urgent); `meetingSummary` (extractive **summary + action items + topics** from "speaker: text" lines); `rankBySimilarity` (a local TF·IDF-cosine "semantic" rank over decrypted history — embeddings ride the model seam); `assistantIntent` (routes summarize/search/canned on-device, defers free-form to a model). Nothing leaves the device. **Web (Thread):** a **toxicity nudge** — before sending high-toxicity wording, a "may come across as offensive — send anyway?" confirm; the **✨ Summarize** sheet (T11.02) is now a meeting recap — summary + **action items** list + **#topic** chips ("generated on your device"). All gated by `aiFeaturesOn()`. VERIFIED: client-core typecheck + **115** tests green; web typecheck clean (only the pre-existing T7.04 regen refs); no server changes. **SEAM:** the free-form **AI chatbot** + **embedding-based** semantic search need a model provider — `assistantIntent`/`rankBySimilarity` are the on-device substrate; a full assistant chat UI + the model route through the T11.01 `AiRuntime`. Mobile rides T5.01. **PHASE 11 COMPLETE.**

# PHASE 12 — Collaboration (V4)

- [x] **T12.01  Shared notes + tasks** *(server + web done; live cursor presence + full CRDT char-merge ride the presence plane / a seam; mobile rides T5.01)*
  PROMPT> internal/collab: shared notes, shared task lists, comments, approvals, version history, presence + activity timeline. Server (CRDT/OT) + clients.
  DONE. **Server `internal/collab`** (hexagonal; migration 000027 collab_notes + collab_note_revisions + collab_tasks + collab_comments + collab_activity): a per-conversation **Notes & Tasks workspace**, gated on `conversation_members`. **Notes** — create/list/update under **optimistic version concurrency** (`domain.CheckVersion`: a stale `base_version` → **409 CONFLICT_STALE** so the client reloads + rebases — a lightweight OT); every accepted edit appends a **revision** (`GET /v1/notes/{id}/revisions` = version history). **Approvals** — a domain state machine (none/rejected→pending→approved|rejected; re-request or decide-non-pending → 409). **Comments** on notes. **Tasks** — add/list/toggle-done/assign/delete. **Activity timeline** — an append-only feed logged on every action (`GET …/activity`, newest first). Routes: `/v1/conversations/{id}/{notes,tasks,activity}` + `/v1/notes/{id}/…`. domain (validation, version rule, approval SM) + service (fakes: versioned edits/409, approval flow, tasks+comments, activity) + **pg integration** (behind WA_TEST_PG_DSN). **Web:** AppServices collab wrappers (409→"changed by someone else"); a **CollabScreen** (opened from the thread header) with **Tasks / Notes / Activity** tabs — task add/check/delete; a **NoteEditor** (versioned Save, Request-approval / Approve-Reject, comments, version History); an activity list. VERIFIED: gofmt + vet + `go build ./cmd/core-api` + `go test ./internal/collab/...` green; web typecheck clean (only the pre-existing T7.04 regen refs); lint-pattern scan clean. **NOTE:** collab content is deliberately-shared **workspace** state (server-visible), not E2EE messages. **SEAM:** live cursor/typing **presence** rides the existing presence plane (T-presence); **full CRDT** character-level merge of concurrent edits is the documented seam — the version-gated edit + revision log is the substance. Mobile rides T5.01.
- [x] **T12.02  Whiteboard + live collaboration** *(server + client-core CRDT + web canvas done — PHASE 12 COMPLETE; WS push + file-collab/shared-calendar ride the op-log/collab substrate; mobile rides T5.01)*
  PROMPT> real-time whiteboard + file collaboration over WS (CRDT), team workspace, shared calendar. Server + clients.
  DONE. **CRDT whiteboard.** **Client-core `whiteboard.ts`** (pure, +6 → **121**): the board is an **append-only op-log** — stroke adds form a **grow-only set**, erases are tombstones, a clear is a seq barrier; `mergeOps` unions by op id (commutative + idempotent), `nextSeq`/`maxSeq` are Lamport clocks, `renderStrokes` reduces the log to visible strokes ordered by (seq, id). Two clients drawing concurrently **converge without coordination** (tested: merge-order independence). **Server `internal/whiteboard`** (migration 000028 board_ops, PK (conversation_id, op_id) for idempotency): `POST …/board/ops` appends a batch (author forced to caller, validated, idempotent) + `GET …/board/ops?since=N` returns ops past a cursor with the new cursor — incremental sync; membership-gated. domain + service (fakes: append/sync/author-force/idempotent) + pg-integration tests. **Web:** a **WhiteboardScreen** (opened from the thread header) — an HTML canvas with a colour palette + pen width + Clear; pointer strokes append locally + POST, a **1.2 s poll** merges peers' ops via the client-core CRDT and redraws; normalised 0..1 points so it scales. AppServices `boardSync`/`boardAppend`. VERIFIED: gofmt + vet + `go build ./cmd/core-api` + `go test ./internal/whiteboard/...` green; client-core typecheck + **121** tests green; web typecheck clean (only the pre-existing T7.04 regen refs); lint-pattern scan clean. **SEAM:** real-time **WS push** (vs the 1.2 s poll) rides the existing NATS/WS nudge; **file collaboration** reuses the same op-log substrate; **shared calendar** extends the T12.01 collab workspace; **team workspace** = the group/community. Mobile rides T5.01. **PHASE 12 COMPLETE.**

# PHASE 13 — Discovery & platform (metadata-only)

- [ ] **T13.01  Elasticsearch for public/metadata search**
  PROMPT> internal/discovery: OpenSearch/Elasticsearch index for channels, public communities, usernames — metadata only, never E2EE content. Server + sync pipeline + tests.
- [ ] **T13.02  Bots, mini-apps, interactive messages**
  PROMPT> bot framework (webhook API), interactive messages (buttons/forms/rich cards), mini-app container, saved templates. Server + clients.

# PHASE 14 — Notifications & multi-channel (expand)

- [ ] **T14.01  Multi-channel notifications**
  PROMPT> email + SMS notification drivers (offline: ntfy already wired), notification channels, snooze, scheduled notifications, sound/vibration settings, desktop notifications. notification-svc + clients.

# PHASE 15 — Scale & ops hardening (evidence-gated)

- [ ] **T15.01  Load + capacity re-baseline**
  PROMPT> run the k6 load suite (fanout/callsurge/ptt + new channel/webinar profiles) at target scale; capture where NATS/Postgres are the bottleneck. Report only.
- [ ] **T15.02  Kafka evaluation (only if T15.01 proves need)**
  PROMPT> IF load shows NATS is the bottleneck for a specific stream, introduce Kafka for that stream behind the existing port; otherwise document why NATS stays. Evidence-gated.
- [ ] **T15.03  Cassandra evaluation (only if T15.01 proves need)**
  PROMPT> IF the partitioned Postgres inbox is the bottleneck, prototype a Cassandra inbox behind the InboxWriter port; otherwise document why Postgres stays. Evidence-gated.
- [ ] **T15.04  CDN + object-storage delivery**
  PROMPT> front MinIO with a CDN for media/thumbnails (signed URLs), progressive/streaming video, background upload/download tuning. media-svc + infra.
- [ ] **T15.05  Payments/monetization backbone**
  PROMPT> internal/payments: premium subscriptions, channel monetization, P2P transfer seam (PSP integration behind a port, never handling raw card data). Server + admin.

# GATES

- **After Phase 5:** the app is a fully usable WhatsApp-class messenger (all V2 features tappable).
- **After Phase 8:** feature-parity with Telegram/Discord community shape.
- **After Phase 12:** Slack/Teams-class collaboration.
- Human gates from V2 still apply before public launch: external pentest, DR drill, 2-week SLO soak.

---

## How to run

Pick the lowest open `[ ]` task and paste its `PROMPT>` (e.g. *"implement task
T5.02 …"*). I build it on the existing V2 code, commit + push, CI verifies, and
we flip the box. Phase 5 first — it turns the finished backend into a usable app.
