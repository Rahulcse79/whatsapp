{
  "name": "@wa/media-pipeline",
  "version": "0.0.0",
  "private": true,
  "description": "Client media pipeline (HLD §9): per-file AES-256-GCM chunked encryption, content hashing, and resumable multipart upload to media-svc. Framework-free; real WebCrypto + injected HTTP/thumbnail ports.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p .",
    "test": "vitest run"
  }
}
